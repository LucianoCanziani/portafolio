---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";

const posts = (await Astro.glob("./proyectos/*.{md,mdx}")).sort(
  (a, b) =>
    new Date(b.frontmatter.pubDate).valueOf() -
    new Date(a.frontmatter.pubDate).valueOf()
);
---

<BaseLayout
  title="Portafolio de Luciano Canziani"
  description="Hola! En esta pagina encontraras un listado de mis proyectos. En ellos podras ver las tecnologias que use, explicacion del funcionamiento, que aprendi mientras lo desarrollaba, repositorio de Github y un deploy en vivo del proyecto para que tengas la experiencia de primera mano."
>
  <div class="pb-12 mt-5">
    <h1 class="text-5xl font-bold portafolio-title">Portafolio de Luciano Canziani</h1>
    <h2 class="text-3xl py-3 font-bold">Front-End Developer</h2>
    <div class="py-2">
      <text class="text-lg">
        Hola! En esta pagina encontraras un listado de mis proyectos. En ellos
        podras ver las tecnologias que use, explicacion del funcionamiento, que
        aprendi mientras lo desarrollaba, repositorio de Github y un deploy en
        vivo del proyecto para que tengas la experiencia de primera mano.
      </text>
    </div>
    <div class="mt-8">
      <a
        class="btn"
        href="https://www.linkedin.com/in/luciano-canziani/"
        target="_blank"
        style="background-color: #f2f7ff; color: #000;"
      >
        Conectemos!</a
      >
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">
      Proyectos {"</>"}
    </div>
  </div>

  {
    posts.map((post) => (
      <>
        <HorizontalCard
          title={post.frontmatter.title}
          img={post.frontmatter.heroImage}
          desc={post.frontmatter.description}
          url={post.url}
          stack={post.frontmatter.stack}
          target="_self"
        />
        <div class="divider my-0" />
      </>
    ))
  }
</BaseLayout>
